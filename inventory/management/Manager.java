package inventory.management;

public class Manager {
	/* I need to find a way to store/read products' data.
	 * Idea 1:
	 *   - Store each product's object data in a line of a .txt file;
	 *   - Read all objects(lines) and store on an ArrayList. */
}